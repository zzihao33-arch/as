# CM-HUB 员工凭证检查器进度（2026-09-19）

基于独立候选 `72387b7` 与 `6f35d7c`。隔离镜像已在专用测试主机 `tyg-api-test / ins-nm8jebfh` 构建并通过 13/13 合成验收，镜像摘要为 `sha256:7646609d5c8d1011d4e24169ef4b8ed73839c702967d74af9c51d05e111d0f51`。API 仅在显式启用且配置不可变 SHA-256 镜像后才注入检查器；缺少配置时拒绝保存。

## 检查路径

- 新 Docker 检查镜像使用一次性网络隔离容器、只读根文件系统、非特权 UID、移除 Linux capabilities、2 GiB 内存、1 CPU、64 PID、256 MiB 私有临时盘和 75 秒硬终止。
- 镜像内用 ClamAV 查病毒，并校验 PDF、PNG/JPEG、DOC/DOCX/XLSX 格式；压缩文件大小和解包读取均受限。宏、加密内容、外部模板、活动 PDF 内容和损坏文件拒绝。
- 病毒库超过 7 天、扫描程序出错、超时或 `.xls` 请求都不会得到“干净”判定；当前 `.xls` 明确不可用并拒绝保存，直到另有通过验收的检查方法。
- 新检查镜像只接受 `mode=check`，无文件挂载或网络。Office 转换与预览仍保持延期。
- 13 项合成验收脚本覆盖现有 11 份文档样本、无害 EICAR 杀毒测试串，以及 `.xls` 故障关闭路径；报告不含文件内容或本机路径。

## 本地验证

- 独立候选阶段 `services/cloud-api/npm test`：76/76 通过；本集成分支后端测试 143/143 通过，18 份迁移校验通过。
- `npm run typecheck`、`npm run build`：通过。
- Python 沙箱脚本语法编译、Node 验收脚本语法检查、`git diff --check`：通过。
- 初次红灯：新增的适配器测试因尚无 `pickupDocumentSandbox.js` 而 TypeScript 编译失败；添加实现后回归通过。

## 尚待测试环境验收

集成分支 `7f0e98c` 已快进推送至 `staging`，测试部署工作流运行 `35454325794` 成功（42 秒）。部署脚本在迁移前核验固定 checker 镜像、隔离测试数据库和仅测试环境配置；`https://api-test.cmhubtool.com/healthz` 返回 `ok: true`，`outboundWebhooks.enabled` 为 `false`。工作流包含 `npm run migrate`，因此本次发布步骤已完成迁移程序；独立读取远程迁移台账的只读命令因服务器 Git 的 `dubious ownership` 保护在输出汇总前退出，未修改该主机 Git 配置。

发布验证：本集成分支云 API 测试 147/147、迁移文件校验 18/18、前端测试 48/48；云 API 和前端类型检查及构建均通过。既有浏览器合成脚本没有通过：它首先在 `实际箱数` 的旧无障碍标签定位处超时；临时改用输入框定位后，又在独立的旧会话续期断言超时。临时测试修改已撤销，正式测试文件保持原样。

真实登录后上传、checker 拒绝样本、授权下载、撤权以及 PDF/图片展示尚未完成。测试站登录页需要测试账号；仓库没有可用测试凭据。继续 E2E 前需提供一个现存的测试仓库账号，或确定可以在 `tyg_integration_test` 中创建并在验收后清理的最低权限合成账号。不要使用生产账号或生产数据。正式生产 `PICKUP_DOCUMENTS_ENABLED` 保持关闭。
