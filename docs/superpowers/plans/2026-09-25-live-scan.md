# 服务器实时查号实施记录

目标：扫描直接查询自有服务器最新单号和面单，不依赖浏览器同步全部映射。
依据：用户在 TYG 任务已选择实时查询方案，并在接续任务要求继续实施。
执行：当前会话内完成；前后端分别保留生产基线，不合并无关工作。

- [x] 后端：scan.use + 已选仓库保护 GET /warehouse/v1/shipments/lookup?trackingNo=；按原单号和转单号索引等值查询，最多两个不同 shipment；无结果返回 null，多结果 409；只允许 READY_TO_PRINT 且当前 PDF 未过期/删除/未就绪。新增全局单号索引迁移。
- [x] 测试：真实关系数据库执行查询覆盖原/转单号、重复别名同票、跨客户重号、无匹配、过期、状态错误、参数校验及更新后的面单。
- [x] 前端：保留共享批次优先；共享批次未命中后实时查 API；只有明确无匹配才能走手工导入文件；请求失败不可回退旧云端索引。移除自动批量同步及错误的总票数显示。
- [x] 打印：查询锁覆盖异步查询和打印；以 shipment 身份防止原/转单号并发重复；当前面单校验及实时拦截失败均停止。缓存故障不阻断已校验在线文件。
- [x] 验证：测试先失败后通过，前后端类型检查/构建，浏览器模拟扫描不调用实体打印机。独立审查后按授权发布顺序先后端、再前端，并记录证据和回退方法。

关键约束：PDF 在自有 COS；全客户仓库可见性遵循现有业务；错误不能导致错打；保留前端 cdf66367 和后端 d01d8a08 修复；实体出纸需现场验收。

执行结果：后端226、前端51测试通过；独立审查两项发现已修复并验证；真实MySQL索引与查询、生产浏览器验收通过。详见 docs/operations/tyg-live-scan-2026-09-25.md。

