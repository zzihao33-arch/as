# CM-HUB｜海外仓工具站前端产品需求文档

> 本文档用于 Google Stitch 原型图生成、产品评审和前端验收。重点描述页面功能、模块结构、关键状态和响应式行为，不包含后端实现方案。

---

## 0. 文档信息

| 项目 | 内容 |
| --- | --- |
| 产品名称 | CM-HUB｜海外仓工具站 |
| 文档类型 | 前端产品需求文档（Stitch 原型输入版） |
| 文档版本 | v1.0 |
| 产品形态 | 桌面端仓库工作台 + 移动端现场辅助页面 |
| 主要运行环境 | Windows Chrome、扫码枪、QZ Tray、本地标签打印机 |
| 移动端环境 | iOS/Android 浏览器，用于打卡、拍照、凭证补录和状态查看 |
| 核心目标 | 统一处理扫码换单、空运提货单、考勤薪酬、账号和权限管理 |
| 设计优先级 | 功能完整性 > 操作稳定性 > 信息清晰度 > 视觉装饰 |

### 0.1 Stitch 使用说明

生成原型时必须遵守以下要求：

1. 这是内部仓库运营后台，不是营销官网，不生成 Hero、客户 Logo 墙、价格方案或销售 CTA。
2. 桌面端以 `1440 × 900` 为主要设计画布，同时输出关键移动页面的 `390 × 844` 版本。
3. 每个页面使用本文档指定的屏幕编号和页面名称，不自行创造新的业务模块。
4. 优先完成模块结构、表格、表单、筛选、状态和操作流程；装饰性插画保持克制。
5. 所有示例数据使用物流和仓库业务数据，不使用 `John Doe`、`Acme` 等通用占位内容。
6. 不使用 Emoji 作为功能图标，统一使用简洁的线性 SVG 图标。
7. 不使用玻璃拟态、霓虹渐变、大面积模糊、强阴影或持续循环动画。
8. 页面间共享统一应用外壳、表格样式、表单规则和状态颜色。

---

# 1. 产品概述

## 1.1 产品定位

CM-HUB 是连接上游物流客户与美国海外仓现场操作的统一运营工作台。

上游客户通过 API 推送空运提货单、物流单据和 PDF 面单；仓库人员在同一个系统中完成：

- 物流数据同步和手工导入兜底
- 扫描头程单号并匹配末端面单
- 拦截名单实时校验
- 通过 QZ Tray 调用本地打印机
- 空运提货单预录、入库、交仓和凭证归档
- 员工考勤、异常申诉和薪酬核算
- 内部账号、角色和功能权限管理

系统面向多人、多电脑共同操作同一批货物的真实仓库环境。业务数据以仓库共享为默认，不按照操作电脑或上游客户划分现场可见范围。

## 1.2 核心产品原则

### 全局共享

- 同一仓库内所有授权工作站可以处理全部上游客户推送的数据。
- A 电脑导入的共享作业批次，B 电脑无需重复导入。
- A 电脑维护的拦截条目，对其他工作站立即生效。
- 客户身份用于标记来源和结果回传对象，不用于限制仓库现场人员的数据读取。

### 业务可追溯

- 扫码、拦截、打印提交、入库、交仓、打卡、审批和权限变更均保留操作人、时间和工作站信息。
- QZ Tray 接受打印任务只能表述为“已提交打印”，不能表述为“物理打印成功”。
- 所有异常和拒绝状态必须给出清晰原因。

### 高频操作优先

- 扫码打单页进入后自动聚焦扫描输入框。
- 高频流程尽量在三步以内完成。
- 页面切换不出现前一页面残影。
- 大批数据处理不能阻塞扫码输入和页面导航。

### 权限最小化

- 用户只看到其角色有权访问的菜单和按钮。
- 上游 API Key 不能用于登录仓库工作台，也不能出现在浏览器中。
- 系统管理员拥有全局权限；仓库主管、操作员和薪酬专员按职责授权。

## 1.3 产品目标

1. 将扫码换单的核心操作压缩为“扫描 → 自动匹配与校验 → 提交打印”。
2. 多工作站共同处理同一批货物时，不重复导入、不重复维护拦截规则。
3. 空运提货单从预报、入库到交仓全程可查，并能关联换单进度和现场凭证。
4. 通过账号、角色和权限将所有业务行为关联到具体操作人。
5. 通过在线考勤数据直接生成薪酬核算结果，取消旧考勤 Excel 导入链路。

## 1.4 非目标

- 不提供公众自助注册。
- 不允许上游客户直接控制仓库打印机。
- 不允许上游客户写入 CM-HUB 打印事件或内部业务状态。
- 不在仓库电脑运行 Node 轮询打印桥。
- 当前不负责税务计算、工资发放或劳动合规判定。
- 当前不保证打印机物理出纸成功，只记录可靠的打印提交事实。
- 移动端不承担大批 Excel/PDF 导入或 QZ 正式打印。

---

# 2. 目标用户与权限

## 2.1 用户角色

| 角色 | 主要职责 | 默认可见模块 | 典型设备 |
| --- | --- | --- | --- |
| 仓库操作员 | 扫码换单、入库、交仓、日常打卡 | 工作概览、扫码打单、空提管理、我的考勤 | 仓库 PC、手机 |
| 仓库主管 | 处理现场异常、审核申诉、管理班次和打卡地 | 操作员模块 + 团队考勤 + 管理配置 | PC、手机 |
| 薪酬专员 | 维护时薪、奖金、油补并导出薪酬 | 考勤记录、薪酬核算 | PC |
| 系统管理员 | 管理账号、角色、权限、系统配置和安全审计 | 全部模块 | PC |
| 上游客户 | 通过 API 推送提货单、物流单据和面单 | 不进入仓库工作台 | 客户服务器 |

## 2.2 前端权限规则

1. 无模块查看权限时，该模块不出现在侧边栏。
2. 有页面查看权限但无操作权限时，可以查看数据，但操作按钮隐藏或明确禁用。
3. 权限变化在下一次接口请求后即时生效，不要求重新登录。
4. 账号被停用或角色失效后，当前会话应立即失效。
5. 系统管理员不能因普通角色删除而失去全局权限。

---

# 3. 核心使用场景

## 3.1 场景 A：上游数据驱动的多人换单

1. 上游客户推送一张空运提货单以及属于该提单的多条物流单据。
2. 上游继续上传每条物流单据对应的 PDF 面单。
3. 所有授权仓库工作站同步获得数据。
4. 操作员扫描包裹上的头程单号。
5. 系统完成拦截校验、唯一映射校验、PDF 面单校验和重复打印校验。
6. 校验通过后，将面单提交给当前工作站的 QZ Tray。
7. 系统更新该空运提货单的换单进度。

## 3.2 场景 B：手工导入兜底

1. 上游客户尚未完成 API 推送。
2. 操作员上传多个 Excel 映射文件，并选择多个 PDF 文件夹或 ZIP。
3. 系统分阶段读取、校验并建立索引。
4. 导入成功后形成仓库共享作业批次。
5. 其他工作站直接处理该批次，无需重复上传。

## 3.3 场景 C：全局拦截

1. 仓库人员录入需要拦截的头程单号或末端快递单号。
2. 拦截条目同步到全部工作站。
3. 任意工作站扫描命中后立即阻止打印。
4. 页面显示拦截原因、创建人和创建时间，并播放拦截音效。

## 3.4 场景 D：多提货单共同入库和交仓

1. 操作员从待入库列表选择多张提货单。
2. 核对实际箱数、包裹数和重量，上传共享入库照片。
3. 系统将多张提货单放入同一入库批次。
4. 装车交仓时，再选择同一车辆上的多张已入库提货单。
5. 上传共享 POD 和装车照，形成交仓批次。
6. 凭证不足时允许交仓，但标记“凭证待补”。

## 3.5 场景 E：考勤和薪酬

1. 固定工作站员工使用摄像头、随机动作和浏览器位置辅助完成打卡。
2. 移动端员工必须通过位置围栏校验后打卡。
3. 系统生成每日考勤结果。
4. 员工在 72 小时内对异常结果提交申诉。
5. 仓库主管审批，不能审批自己的申诉。
6. 薪酬专员按周期计算常规工时、OT、奖金和油补，并导出核对表。

---

# 4. 整体信息架构

## 4.1 桌面端导航

### 运营中心

- 工作概览
- 扫码打单
  - 扫码核心区
  - 数据导入
  - 拦截名单
  - 操作日志

### 单据与结算

- 空提管理
  - 提货单列表
  - 入库批次
  - 交仓凭证
- 考勤薪酬
  - 我的打卡
  - 考勤记录
  - 异常申诉
  - 管理配置
  - 薪酬核算

### 管理中心

- 账户管理
- 角色配置

### 系统设置

- 打印机
- 音效设置
- 系统状态

## 4.2 全局应用外壳

所有登录后页面共享同一个应用外壳：

1. **左侧导航栏**
   - 产品标识
   - 按业务域分组的导航
   - 当前仓库及云端同步说明
2. **顶部上下文栏**
   - 面包屑
   - 主题切换
   - 当前运行环境
   - 当前用户、角色和退出入口
3. **主内容区**
   - 页面标题、说明和主要操作
   - 页面统计、筛选、列表或工作区
4. **全局反馈层**
   - Toast
   - 确认弹窗
   - 错误边界
   - 全局网络状态提示

## 4.3 移动端导航

- 左侧导航折叠为顶部抽屉。
- 高频移动操作可使用底部导航，最多五项：概览、空提、打卡、记录、我的。
- 扫码打单、复杂角色配置和大批文件导入不作为移动端主要入口。

---

# 5. 屏幕清单

| 编号 | 页面/屏幕 | 桌面端 | 移动端 | 主要角色 |
| --- | --- | --- | --- | --- |
| AUTH-01 | 登录页 | 必须 | 必须 | 全部内部用户 |
| HOME-01 | 工作概览 | 必须 | 简化版 | 全部内部用户 |
| SCAN-01 | 扫码打单工作台 | 必须 | 只读简化版 | 操作员、主管 |
| SCAN-02 | 数据导入弹窗 | 必须 | 不提供批量导入 | 操作员、主管 |
| SCAN-03 | 拦截名单抽屉 | 必须 | 必须 | 操作员、主管 |
| AIR-01 | 空提管理列表 | 必须 | 必须 | 操作员、主管 |
| AIR-02 | 提货单录入/编辑 | 必须 | 必须 | 操作员、主管 |
| AIR-03 | 批量入库确认 | 必须 | 必须 | 操作员、主管 |
| AIR-04 | 批量交仓确认 | 必须 | 必须 | 操作员、主管 |
| AIR-05 | 提货单详情 | 必须 | 必须 | 操作员、主管 |
| AIR-06 | 交仓凭证列表 | 必须 | 必须 | 操作员、主管 |
| ATT-01 | 我的打卡 | 必须 | 必须 | 全部员工 |
| ATT-02 | 考勤记录 | 必须 | 必须 | 员工、主管、薪酬专员 |
| ATT-03 | 异常申诉 | 必须 | 必须 | 员工、主管 |
| ATT-04 | 考勤管理配置 | 必须 | 只读/简化 | 主管、系统管理员 |
| ATT-05 | 薪酬核算 | 必须 | 只读摘要 | 薪酬专员、系统管理员 |
| ADMIN-01 | 账户管理 | 必须 | 简化版 | 系统管理员 |
| ADMIN-02 | 角色配置 | 必须 | 不作为主要场景 | 系统管理员 |
| SET-01 | 打印机设置 | 必须 | 只读状态 | 操作员、主管 |
| SET-02 | 音效设置 | 必须 | 必须 | 操作员、主管 |
| SET-03 | 系统状态 | 必须 | 摘要版 | 主管、系统管理员 |

---

# 6. 页面详细需求

## AUTH-01 登录页

### 页面目标

完成仓库内部用户身份认证，并安全进入其有权访问的第一个业务页面。

### 桌面端模块结构

1. 居中登录容器，最大宽度 520px。
2. 品牌标识：CM-HUB 图标、产品名称“CM-HUB｜海外仓工具站”。
3. 登录说明：使用内部员工账号登录，上游 API Key 不应填写在浏览器中。
4. 登录表单：
   - 登录账号
   - 密码
   - 显示/隐藏密码
   - 记住账号
   - 登录按钮
5. 网络和表单错误区。
6. 页面右下角版本号。

### 关键交互

- 支持密码管理器、粘贴和回车提交。
- 登录按钮在请求期间显示“正在登录”并禁用。
- 错误凭据统一提示“账号或密码错误”。
- 账号异常统一提示“账户异常，请联系管理员”。
- 首次登录强制修改临时密码。
- 会话失效后返回登录页，并在重新登录后回到原目标页面。

### 状态设计

- **初始状态**：空表单，默认记住账号。
- **会话恢复**：品牌区域保持显示，表单位置使用固定尺寸骨架。
- **网络错误**：表单内容不清空，提供重新登录按钮。
- **账号锁定**：显示剩余锁定时间，不展示具体密码错误次数。

### 移动端

- 单列满宽布局，左右安全间距 20px。
- 输入框和主按钮高度至少 48px。
- 软键盘弹出时登录按钮不能被遮挡。

---

## HOME-01 工作概览

### 页面目标

根据当前用户权限展示今日业务摘要、待处理任务和高频入口。

### 模块结构

1. 页面标题及当前仓库名称。
2. 今日业务摘要：
   - 待换单包裹
   - 已提交打印
   - 拦截数量
   - 待入库提货单
   - 凭证待补
   - 考勤异常
3. 快捷操作：
   - 进入扫码打单
   - 录入提货单
   - 批量入库
   - 我的打卡
4. 连接状态：
   - 云端同步
   - QZ Tray
   - 当前打印机
   - 本地缓存
5. 待处理任务列表。
6. 最近操作摘要。

### 关键交互

- 统计卡可点击进入对应筛选结果。
- 无权限统计项不显示。
- 数值更新时保持卡片宽度稳定，不出现布局跳动。
- 连接异常必须提供直接处理入口。

### 状态设计

- **首次使用**：显示“完成工作站、打印机和班次配置”的引导清单。
- **无待办**：显示“当前没有待处理任务”，保留快捷入口。
- **加载**：统计卡使用固定高度骨架，列表保留标题结构。
- **部分失败**：单个模块失败不阻塞其他模块，失败卡片提供重试。

### 移动端

- 摘要卡使用两列网格。
- 连接状态合并为一张状态卡。
- 最近操作默认折叠，只显示前三条。

---

## SCAN-01 扫码打单工作台

### 页面目标

让操作员通过扫码枪连续、稳定地完成物流单据匹配、拦截校验和打印提交。

### 模块结构

1. **顶部信息卡**
   - 映射条目数量
   - PDF 面单数量
   - 已提交打印数量
   - 当前共享批次或云端来源
2. **快捷操作**
   - 数据导入
   - 拦截名单
   - 打印机设置
   - 音效设置
3. **扫码核心区**
   - 扫码并打印说明
   - 扫描单号输入框
   - 扫码枪连接状态
   - 当前打印机
   - Enter 提交提示
   - 打印按钮
4. **本次处理结果区**
   - 扫描单号
   - 匹配到的末端单号
   - 来源客户
   - 处理结果
   - 阻断原因
5. **操作日志**
   - 打印记录
   - 导入记录
   - 系统状态
   - 清空当前本机日志

### 关键交互

- 页面进入后自动聚焦扫描输入框。
- 扫码枪输入并回车后自动提交。
- 每次处理完成后恢复输入焦点并清空输入框。
- 只有唯一匹配、面单存在、未命中拦截且未被重复保护阻断时才允许提交打印。
- 五分钟内重复扫描默认阻断，人工覆盖必须明确确认并留痕。
- QZ 接收后显示“已提交到打印机”，不能显示“打印成功”。
- 扫描、拦截、缺面单、多候选和 QZ 异常使用不同视觉状态和音效。
- 打印提交期间只锁定当前任务，不使用全屏遮罩。
- 页面切换回来时保持已加载数据和输入焦点，不出现残影。

### 结果状态

| 状态 | 视觉语义 | 文案示例 |
| --- | --- | --- |
| 待扫描 | 中性蓝灰 | 等待扫码或输入单号 |
| 匹配中 | 蓝色进度 | 正在匹配面单与拦截规则 |
| 已提交 | 绿色图标 + 文字 | 已提交到 Brother DCP-L2640DW Printer |
| 已拦截 | 红色图标 + 原因 | 命中拦截名单，已阻止打印 |
| 缺少面单 | 橙色警告 | 已找到映射，但未找到对应 PDF |
| 多候选 | 红色警告 | 匹配结果不唯一，请人工处理 |
| QZ 异常 | 红色错误 | 无法连接本机 QZ Tray |

### 空状态

- 没有云端或共享批次数据时，显示“等待云端同步或导入本地数据”。
- 没有操作日志时，显示“扫描后将在这里产生可追溯记录”。
- 没有打印机时，打印按钮禁用并显示配置入口。

### 加载状态

- 首次同步显示已读取数量和总数量。
- 大批 PDF 下载按批次显示进度。
- 扫码热路径不得等待完整表格渲染。

### 移动端

- 只提供统计、日志和拦截信息查看。
- 不提供 QZ 正式打印和大批文件导入。

---

## SCAN-02 数据导入弹窗

### 页面目标

在上游 API 暂无数据时，完成手工映射和面单文件导入。

### 模块结构

1. 弹窗标题“数据导入与面单库”。
2. 导入摘要标签：Excel 数量、PDF 来源数量、PDF 文件总数。
3. Excel 映射卡：
   - 点击或拖拽多个 Excel
   - 已选文件列表
   - 解析结果和错误数量
4. PDF 面单卡：
   - 添加文件夹
   - 添加 ZIP
   - 已添加来源列表
   - 文件总数
5. 规则说明：
   - 旧模板：头程单号 → 快递单号
   - 新模板：运单号 → 参考单号
   - 同名 PDF 和冲突映射必须处理后才能导入
6. 分阶段进度区。
7. 取消和确认导入按钮。

### 关键交互

- 支持多个 Excel、多个文件夹和多个 ZIP 累计添加。
- PDF 累计最多 20,000 个。
- 同一弹窗内重复选择文件时去重。
- 导入过程分为“读取文件、校验映射、保存资产、建立索引”四个阶段。
- 冲突错误展示具体文件、行号和原因。
- 关闭弹窗时若有未提交文件，显示放弃确认。
- 导入成功后形成共享作业批次，并更新扫码工作台数量。

### 空状态

- 左右上传卡分别显示支持格式、数量和用途。
- PDF 操作区即使尚未选择文件，也必须显示“添加文件夹”和“添加 ZIP”。

### 加载状态

- 不使用无限旋转图标代替长任务进度。
- 显示当前阶段、已处理数量、总数和失败数量。
- 用户可以在处理期间查看错误，但不能重复提交。

### 移动端

- 不提供文件夹和 ZIP 批量导入。
- 仅展示当前共享批次状态。

---

## SCAN-03 拦截名单抽屉

### 页面目标

在扫码打单上下文中维护并查看全局拦截条目。

### 模块结构

1. 抽屉标题和拦截说明。
2. 单号输入框。
3. 扫描添加、批量粘贴和添加按钮。
4. 拦截列表：单号、原因、创建人、创建时间、状态。
5. 搜索和筛选。
6. 删除操作。

### 关键交互

- 支持头程单号和末端快递单号。
- 批量粘贴按换行、空格或逗号解析。
- 重复单号提示已存在，不重复创建。
- 删除拦截条目必须确认。
- 拦截存储或同步状态异常时，扫码打印采用 fail-closed：阻止打印并提示系统异常。

### 空状态

显示“当前没有拦截条目”，并解释新增后将对所有工作站生效。

### 移动端

- 抽屉改为全屏页面。
- 添加区固定在顶部，列表使用卡片。

---

## AIR-01 空提管理列表

### 页面目标

统一展示空运提货单从预报、入库到交仓及换单处理的当前状态。

### 模块结构

1. 页面标题“空提管理”和说明。
2. 主要操作“录入提货单”。
3. 统计条：
   - 当前结果
   - 已选择
   - 待入库
   - 凭证待补
4. 状态标签页：
   - 全部
   - 已录入
   - 已入库
   - 已交仓
   - 已作废
5. 搜索及筛选：
   - 提货单号
   - 来源客户
   - 凭证状态
   - 更新时间
6. 批量操作提示和操作栏。
7. 提货单表格：
   - 多选框
   - 提货单号
   - 来源客户
   - 预报信息
   - 换单进度
   - 状态
   - 批次
   - 更新时间
   - 操作
8. 分页。

### 业务显示规则

- 提货单号 `abc-123`、`ABC-123`、`ABC123` 和 `abc123` 视为同一单号。
- 页面展示时优先保留标准格式，例如 `180-98109734`。
- 不在列表展示低价值的货物名称列。
- 更新时间格式使用 `8.29.26 18:30:26`，保持单行。
- 来源客户必须展示，并可用于筛选。

### 换单进度组件

使用紧凑进度条或圆环，展示：

- 总物流单据数
- 已提交打印数
- 已拦截数
- 待处理数

示例：`1,642 / 2,000 · 82%`。

“已提交打印”不等于物理出纸确认，组件提示中必须说明统计口径。

### 关键交互

- 点击提货单号进入详情。
- 已录入提货单提供“入库、编辑、作废”。
- 已入库提货单提供“交仓、详情”。
- 已交仓提货单提供“详情、补充凭证”。
- 批量入库只接受已录入提货单。
- 批量交仓只接受已入库提货单。
- 选择状态不一致的单据时，明确说明不可批量处理的原因。

### 空状态

- 无任何提货单：引导录入或等待上游客户推送。
- 当前筛选无结果：保留筛选条件，并提供“清除筛选”。
- 无换单数据：显示“客户尚未推送物流单据”。

### 加载状态

- 保留表头和筛选区，表格主体使用行骨架。
- 换单进度独立延迟加载，不阻塞列表文本。
- 列表超过 50 条时分页或虚拟化。

### 移动端

每张提货单转为卡片：

1. 提货单号和状态。
2. 来源客户和预报数据。
3. 换单进度条。
4. 批次和更新时间。
5. 主要操作按钮。

批量选择后使用固定底部操作栏。

---

## AIR-02 提货单录入/编辑

### 页面目标

创建空运提货单预报或修改仍处于已录入状态的预报信息，并集中留存运营交接给司机的提货文件。

### 模块结构

1. 基础信息：
   - 提货单号，必填，全局唯一，最多 32 字符
   - 归属客户，必填；业务客户或上游客户二选一
   - 货物名称，选填，最多 100 字符
2. 预报信息：
   - 预报箱数，1～999999
   - 预报包裹数，1～999999
   - 预报重量，大于 0
   - 重量单位，KG 或 LB
3. 提货文件，选填：PDF、DOC、DOCX、XLS、XLSX、CSV；每单最多 10 个，单个最多 20MB。
4. 备注，最多 200 字符。
5. 保存和取消。

### 关键交互

- 提货单号失焦后完成规范化和唯一性校验。
- 归属客户必须从有效客户档案选择；业务客户与上游客户不可在同一张提货单上同时选择。
- 有客户档案管理权限的用户可在录入窗口新增业务客户，或新增处于待对接/对接中的上游客户。
- 上游客户完成系统对接后，系统按客户编码连接集成身份；既有手工单仍保留手工录入审计。
- 客户档案管理支持删除未被任何提货单引用、且未绑定集成身份的客户；其余客户必须保留以确保业务追溯。
- 提货文件在提货单创建后上传并私有留存；授权运营人员可在详情下载，也可补传。主管或系统管理员移除时必须验证当前密码并填写原因，原文件按留存策略保留。
- 表单错误显示在字段下方。
- 未保存内容在本机暂存 60 分钟。
- 离开有修改的表单前显示确认。
- 已入库后不能再通过此页面修改预报字段。

### 状态设计

- 客户列表加载失败时禁用保存并提供重试。
- 重复提货单号显示现有单据入口。
- 保存中锁定表单，成功后返回详情或列表。

### 移动端

- 单列布局。
- 数字字段使用数字键盘。
- 保存按钮固定底部时，内容必须预留底部空间。

---

## AIR-03 批量入库确认

### 页面目标

将一个或多个已录入提货单放入同一个入库批次，并保存实际数据和入库照片。

### 模块结构

1. 已选提货单摘要。
2. 每张提货单的预报与实际数据对比：
   - 实际箱数
   - 实际包裹数
   - 实际重量及单位
   - 差异说明
3. 入库批次信息。
4. 共享入库照片上传区。
5. 照片缩略图、预览和删除。
6. 确认入库按钮。

### 关键交互

- 实际值默认带入预报值。
- 任意实际值发生变化时，差异说明自动变为必填。
- 多张提货单共同引用同一批次的入库照片，不重复上传文件。
- 图片允许 JPG、JPEG、PNG；必须校验真实文件类型。
- 单张不超过 10MB；前端先压缩和生成预览。
- 某张图片上传失败时，已经上传成功的图片保持不变。
- 提交期间按钮显示“正在保存入库批次”，防止重复提交。

### 空状态

- 未选择提货单时不进入该页面。
- 未上传照片时显示拍照/选择图片入口和用途说明。

### 移动端

- 优先显示“拍照”和“从相册选择”。
- 图片使用三列缩略图网格。
- 每张提货单的实际数据使用可折叠区块。

---

## AIR-04 批量交仓确认

### 页面目标

将一个或多个已入库提货单放入同一个交仓批次，并归档 POD 和装车照。

### 模块结构

1. 已选提货单和合计信息。
2. 交仓批次信息。
3. POD 上传区，0～9 张。
4. 装车照上传区，0～9 张。
5. 凭证完整度提示。
6. 确认交仓按钮。

### 凭证规则

- 完整凭证：POD 至少 1 张，装车照至少 3 张。
- 允许无照片或照片不足时先确认交仓。
- 凭证不完整时，交仓批次和相关提货单标记“凭证待补”。
- 后续可以补充、删除或替换凭证，但必须记录操作人和时间。

### 关键交互

- 上传后立即显示缩略图。
- 点击缩略图打开全屏预览，支持左右切换。
- 确认交仓前再次显示已选单据数量和凭证状态。
- 凭证不完整时使用警告确认，不使用阻断错误。
- 已交仓状态不可撤回。

### 移动端

- 拍照入口优先。
- POD 和装车照分为两个明确区域。
- 底部固定确认栏显示“共 N 张提货单”和凭证完整度。

---

## AIR-05 提货单详情

### 页面目标

集中查看单张提货单的基本信息、物流换单进度、状态流转和所有现场凭证。

### 模块结构

1. 页面标题：提货单号、来源客户、当前状态。
2. 预报和实际信息对比。
3. 换单进度：
   - 总单据
   - 已提交打印
   - 已拦截
   - 待处理
4. 所属物流单据摘要，可进入明细。
5. 状态流转时间轴：
   - 已录入
   - 已入库
   - 已交仓
   - 已作废
6. 入库照片缩略图。
7. POD 和装车照缩略图。
8. 操作审计：操作人、时间、工作站、差异说明。

### 关键交互

- 时间轴中的入库和交仓节点直接展示凭证缩略图。
- 图片延迟加载，但最终必须展示缩略图。
- 已交仓且凭证不完整时显示明显的“补充凭证”入口。
- 物流单据较多时只展示摘要和前若干条，支持进入独立明细。

### 空状态

- 无物流单据：客户尚未推送包裹数据。
- 无入库照片：本次入库未留存照片。
- 无交仓凭证：凭证待补。

### 移动端

- 使用纵向信息卡和时间轴。
- 图片预览占满屏幕。

---

## AIR-06 交仓凭证列表

### 页面目标

按交仓批次集中查询多张提货单共同使用的 POD 和装车凭证。

### 模块结构

1. 批次搜索。
2. 凭证状态筛选：全部、完整、待补。
3. 交仓日期筛选。
4. 批次列表：
   - 批次号
   - 提货单数量
   - 来源客户
   - POD 数量
   - 装车照数量
   - 凭证状态
   - 交仓时间
   - 操作人
5. 批次详情和图片预览。

### 空状态

无交仓批次时说明需要先在空提管理中完成交仓。

### 移动端

列表改为批次卡片，图片数量和凭证状态作为主要信息。

---

## ATT-01 我的打卡

### 页面目标

让员工在十秒内完成合规上班或下班打卡。

### 模块结构

1. 页面标题和服务器时区 `America/New_York`。
2. 摄像头现场画面。
3. 随机动作提示：眨眼或张嘴。
4. 环境预检：
   - 摄像头
   - 浏览器位置
   - 网络
   - 当前工作站
5. 今日状态：上班时间、下班时间、当前进度。
6. 打卡按钮。
7. 隐私和证据保留说明。

### 关键交互

- 固定工作站：摄像头和工作站必须有效，浏览器位置只作为辅助信号。
- 移动端：必须获得浏览器位置并通过打卡围栏。
- 系统随机要求眨眼或张嘴，不做人脸身份比对，不保存人脸模板。
- 动作通过后才能提交当前打卡。
- 一天只接受一组上班/下班打卡。
- 跨午夜下班归属上班日期，连续时长最多 18 小时。
- 打卡成功显示服务器时间和现场照片缩略图。

### 状态设计

- 摄像头启动中。
- 摄像头不可用。
- 等待动作。
- 动作通过。
- 获取位置中。
- 位置在范围外。
- 正在提交。
- 打卡成功。
- 打卡被拒绝并说明原因。

### 移动端

- 画面、动作、位置和打卡按钮纵向排列。
- 主按钮固定在安全区域上方。
- 不显示复杂团队或薪酬内容。

---

## ATT-02 考勤记录

### 页面目标

员工查看本人记录；主管和授权人员查看当前仓库团队记录。

### 模块结构

1. 日期范围。
2. 本人/团队范围提示。
3. 状态筛选：正常、迟到、早退、缺卡、待复核。
4. 考勤表格：
   - 日期
   - 员工
   - 上班
   - 下班
   - 净工时
   - 状态
   - 异常
   - 现场抓拍
   - 更新时间
5. 现场照片预览。
6. 异常申诉入口。

### 关键交互

- 员工只能查看本人记录。
- 团队查看权限由后端控制。
- 点击异常状态进入申诉。
- 现场照片使用权限校验后的私有资源。
- 照片缩略图懒加载，并预留固定尺寸。

### 空状态

- 所选日期无记录。
- 当前筛选无匹配记录。
- 无权查看团队记录。

### 移动端

- 记录转为按日期分组的卡片。
- 默认显示本人记录。

---

## ATT-03 异常申诉

### 页面目标

员工在异常产生后 72 小时内提交修正申请，由仓库主管处理。

### 模块结构

1. 申诉列表及状态标签。
2. 异常记录摘要。
3. 新建申诉表单：
   - 异常类型
   - 原因说明
   - 期望上班时间
   - 期望下班时间
4. 主管审核面板：
   - 原始结果
   - 申请修正
   - 现场凭证
   - 通过/驳回
   - 审核备注

### 关键交互

- 超过 72 小时不允许新建申诉，并说明原因。
- 员工不能重复提交同一异常的未结申诉。
- 主管不能审批自己的申诉。
- 审批通过后保留修正前后快照。
- 审批结果即时更新每日考勤结果。

### 状态设计

- 待审核：橙色标签 + 文字。
- 已通过：绿色标签 + 文字。
- 已驳回：红色标签 + 文字。
- 已过期：中性灰标签 + 文字。

### 移动端

- 表单单列。
- 原始值和申请值使用上下对比，不使用宽表格。

---

## ATT-04 考勤管理配置

### 页面目标

配置当前仓库的打卡地点、班次和宽限规则。

### 模块结构

1. 打卡地列表：
   - 名称
   - 地址
   - 经纬度
   - 半径
   - 状态
2. 新增/编辑打卡地：
   - 名称
   - 手动经纬度
   - 围栏半径 50～1000 米
3. 班次规则列表：
   - 生效日期
   - 工作日
   - 上班时间
   - 下班时间
   - 迟到宽限
   - 早退宽限
4. 新增/编辑班次。

### 关键交互

- 当前版本不提供地图，使用坐标、精度、距离和命中状态。
- 班次按生效日期保存，不直接覆盖历史规则。
- 工作日使用周一至周日多选。
- 停用打卡地前提示影响。

### 空状态

- 无打卡地：员工移动端不能打卡，提供新增入口。
- 无班次：无法计算迟到和早退，提供新增入口。

### 移动端

- 主管可查看配置摘要。
- 复杂编辑以桌面端为主要场景。

---

## ATT-05 薪酬核算

### 页面目标

从云端考勤日结果生成可核对、可导出的薪酬计算快照。

### 模块结构

1. 核算周期：自然月或自定义日期。
2. 人员筛选。
3. 时薪档案管理。
4. 奖金和油补调整。
5. 核算摘要：
   - 员工数
   - 常规工时
   - OT 工时
   - 计算阻断数量
6. 薪酬明细表：
   - 姓名
   - 工号
   - 周工时
   - 常规工时
   - OT 工时
   - 常规工资
   - 加班工资
   - 奖金
   - 油补天数
   - 油补金额
   - 应发金额
   - 核对
7. 固化快照和导出 Excel。

### 计算规则展示

- 每组完整上下班记录固定扣除 1 小时午休。
- 每周一至周日统计。
- 每周前 40 小时为常规工时。
- 超过 40 小时的部分按基础时薪 1.5 倍计算。
- 油补金额为 `$19.50 × 油补天数`。
- 奖金由授权人员手工维护。
- 缺少时薪或存在未处理异常时标记“计算阻断”。

### 关键交互

- 预览不会直接产生最终文件。
- 导出前先固化不可变薪酬快照。
- 存在计算阻断时禁止导出，并列出问题员工。
- 导出工作表名称为“考勤及工时汇总”。
- 最后一列固定为“核对”，无异常显示“已核对”。

### 空状态

- 无考勤数据。
- 尚未配置员工时薪。
- 当前周期没有符合条件的员工。

### 移动端

- 仅提供核算摘要和个人结果查看。
- 完整核算、维护和导出使用桌面端。

---

## ADMIN-01 账户管理

### 页面目标

完成仓库员工账号的创建、角色分配、状态管理和密码重置。

### 模块结构

1. 搜索：登录名、姓名、工号或手机号。
2. 角色和状态筛选。
3. 账户列表：
   - 姓名
   - 登录账号
   - 工号
   - 手机号
   - 角色
   - 状态
   - 最近登录
   - 操作
4. 新增/编辑账户抽屉。
5. 禁用、启用、重置密码、删除操作。

### 关键交互

- 登录账号和工号必须唯一。
- 新账号首次登录必须修改临时密码。
- 禁用后当前会话立即失效。
- 删除账号前提示该账号的历史操作记录不会随业务事实删除。
- 不在前端展示或保存明文密码。
- 当前系统管理员不能误删自己最后一个可用管理身份。

### 空状态

- 尚无员工账号：显示新增第一个员工入口。
- 筛选无结果：提供清除筛选。

### 移动端

- 列表改为账户卡片。
- 低频操作进入“更多”菜单。

---

## ADMIN-02 角色配置

### 页面目标

按岗位职责创建角色并分配菜单和操作权限。

### 模块结构

1. 角色列表：名称、说明、状态、关联员工数。
2. 角色详情。
3. 两级权限树：
   - 一级业务模块
   - 二级查看、创建、修改、删除、导出、审批权限
4. 权限变更摘要。
5. 保存和删除角色。

### 关键交互

- 角色名称全局唯一，最多 20 个中文字符。
- 勾选一级模块可全选子权限，取消时清除子权限。
- 保存前显示新增和移除的权限差异。
- 删除角色时提示受影响员工数量。
- 删除后关联员工变为未分配角色并禁止登录，系统管理员身份除外。

### 空状态

展示系统预设角色和“新建角色”入口。

### 移动端

- 角色列表和权限详情拆成两级页面。
- 完整权限树配置以桌面端为主。

---

## SET-01 打印机设置

### 页面目标

检测本机 QZ Tray，选择实际标签打印机并完成测试打印。

### 模块结构

1. QZ Tray 连接状态。
2. 已检测打印机列表。
3. 当前打印机。
4. 刷新设备。
5. 打印参数。
6. 测试打印。

### 关键交互

- 过滤虚拟打印机和 PDF 打印设备。
- 检测到多个实体打印机时必须人工选择。
- 测试打印与业务打印明确区分。
- QZ 离线、授权失败、无设备、连接超时分别显示不同错误。

### 空状态

- 未安装 QZ Tray。
- QZ 未启动。
- 未检测到实体打印机。
- 尚未选择默认打印机。

### 移动端

仅展示当前工作站打印状态，不允许选择移动设备打印机。

---

## SET-02 音效设置

### 页面目标

配置扫码现场的成功、失败和拦截反馈音效。

### 模块结构

1. 音效总开关。
2. 音量 0～100。
3. 成功音效试听。
4. 失败音效试听。
5. 拦截音效试听。
6. 现场增强模式。

### 关键交互

- 调整音量时即时反馈。
- 试听操作不能触发任何业务状态。
- 长按失败音效试听应有明确文字说明。

### 移动端

提供相同设置，但说明只影响当前设备。

---

## SET-03 系统状态

### 页面目标

帮助现场人员快速判断问题来自云端、本机浏览器、QZ Tray、打印机还是数据同步。

### 模块结构

1. 云端 API 状态。
2. 数据同步状态。
3. QZ Tray 状态。
4. 当前打印机状态。
5. 本地缓存状态。
6. 私有面单服务状态。
7. 最近异常摘要。
8. 重新检测按钮。

### 关键交互

- 每个检测项显示状态、最近检测时间和建议操作。
- 重新检测只更新状态，不刷新整个页面。
- 系统状态页不使用无法解释的总分或百分比健康度。

### 移动端

只展示连接状态摘要和建议处理方式。

---

# 7. 统一视觉设计规范

## 7.1 视觉氛围

产品视觉应呈现“稳定、专业、克制、数据优先”的仓库运营后台：

- **密度：8/10，Cockpit Dense。** 信息较多，但通过网格、分组和留白保持可扫描性。
- **变化：2/10，Predictable Symmetric。** 页面结构一致，操作位置稳定。
- **动效：2/10，Static Restrained。** 仅使用必要的状态过渡，不使用展示型动画。
- **风格关键词：** Minimal Swiss、企业 SaaS、浅色、网格化、高对比、低阴影、细边框。

## 7.2 颜色系统

全产品最多使用一个品牌强调色。橙色、红色和绿色仅作为语义状态色，不作为第二品牌色。

| 语义名称 | 色值 | 用途 |
| --- | --- | --- |
| Canvas Mist | `#F8FAFC` | 页面背景 |
| Pure Surface | `#FFFFFF` | 卡片、表格、弹窗表面 |
| Charcoal Ink | `#172033` | 主标题和主要正文，不使用纯黑 |
| Slate Text | `#475569` | 次要说明和元数据 |
| Whisper Border | `#E2E8F0` | 边框、表格分割线 |
| CM-HUB Blue | `#2563EB` | 主按钮、选中状态、焦点环 |
| Soft Blue | `#EFF6FF` | 当前行、选中卡片和辅助背景 |
| Success Green | `#16A34A` | 成功和完成状态 |
| Warning Amber | `#D97706` | 待处理和凭证待补 |
| Danger Red | `#DC2626` | 阻断、删除和错误 |

设计要求：

- 普通文字与背景对比度至少 4.5:1。
- 状态不能只通过颜色表达，必须同时使用文字和图标。
- 禁止紫色霓虹、蓝紫渐变、彩色外发光。
- 深色模式使用同一语义令牌映射，不单独创造另一套风格。

## 7.3 字体

- **中文和正文：** `Geist Sans` 优先，中文回退使用 `PingFang SC`、`Microsoft YaHei`、系统无衬线字体。
- **数字、单号、时间：** `Geist Mono` 或 `IBM Plex Mono`。
- **页面标题：** 28～32px，600 权重。
- **模块标题：** 18～20px，600 权重。
- **正文：** 桌面端 14～16px，移动端不小于 16px。
- **辅助信息：** 不小于 12px，且保持可读对比度。
- 不使用衬线字体，不使用营销型超大标题。

## 7.4 布局和间距

- 桌面主内容最大宽度 1600px，常规页面左右内边距 32～40px。
- 使用 4/8px 间距体系：4、8、12、16、24、32、40、48。
- 页面标题到第一模块间距 24～32px。
- 高密度表格行高 52～60px。
- 卡片圆角 8～12px；弹窗和抽屉圆角 12px。
- 卡片以 1px 细边框区分层级，阴影仅用于弹窗、抽屉和悬浮反馈。

## 7.5 组件规范

### 按钮

- 主按钮：CM-HUB Blue 填充，白色文字。
- 次按钮：白色背景、浅灰边框。
- 危险按钮：仅在确认语境使用红色。
- 按下状态使用颜色和轻微内阴影反馈，不产生布局位移。
- 桌面按钮高度 40px，移动端至少 48px。

### 表单

- 标签始终位于输入框上方。
- 帮助文字位于输入框下方。
- 错误文字紧邻对应字段。
- 不使用只有 placeholder 而无标签的输入框。
- 必填项统一使用清晰标记。

### 表格

- 表头使用浅灰背景和中等字重。
- 数字和时间列右对齐或使用等宽字体。
- 行操作固定在最右侧。
- 表格滚动时表头可吸顶，但不能遮挡键盘焦点。
- 批量选择后在表格上方或底部显示明确操作栏。

### 状态标签

- 标签包含文字，不能只显示彩色圆点。
- 同一业务状态全系统使用相同颜色和命名。
- “已提交打印”和“物理打印成功”必须严格区分。

### 图片凭证

- 缩略图使用固定长宽比，避免加载后页面跳动。
- Hover 显示预览提示，移动端点击直接进入预览。
- 图片加载失败显示失败占位和重试，不显示破损图片图标。

### 加载器

- 使用与最终结构一致的骨架屏。
- 禁止在长列表或整页使用单个大号圆形 Spinner。
- 超过一秒的任务显示阶段和进度。

## 7.6 动效

- 页面切换不添加整体淡入、滑入或缩放动画。
- 弹窗和抽屉使用 150～220ms 的 opacity/transform 过渡。
- Toast 使用 150ms 进入、120ms 离开。
- 禁止持续脉冲、浮动、闪烁和瀑布式列表入场。
- 支持 `prefers-reduced-motion`，减少动态时直接呈现最终状态。

## 7.7 禁止模式

- 不使用 Emoji 图标。
- 不使用纯黑 `#000000`。
- 不使用玻璃拟态或模糊透明卡片。
- 不使用霓虹外发光。
- 不使用营销型 Hero 和巨大标题。
- 不使用三张等宽营销功能卡。
- 不使用大面积渐变文字。
- 不使用悬停位移、弹跳或抖动。
- 不使用“Elevate、Unleash、Next-Gen”等营销文案。
- 不在操作后台加入装饰性照片或无业务价值的插画。
- 不生成虚构 KPI，如 `99.99%` 或无来源的增长百分比。

---

# 8. 统一交互规范

## 8.1 导航与页面切换

- 当前导航项使用浅蓝背景、蓝色文字和清晰图标。
- 页面切换立即替换主内容，不保留前一页面视觉层。
- 面包屑与侧边栏当前项保持一致。
- 返回操作保留合理的筛选条件和页码。
- 临时敏感数据、密码和打卡照片不跨页面保留。

## 8.2 表单提交

- 提交后按钮显示进行中状态并禁用。
- 成功 Toast 只作为补充，页面真实数据必须同步更新。
- 失败时保留用户已经填写和选择的内容。
- 多字段错误在字段附近展示；必要时顶部同时显示错误摘要并聚焦。

## 8.3 危险操作

以下操作必须二次确认：

- 删除账号
- 删除角色
- 删除拦截条目
- 作废提货单
- 确认交仓
- 覆盖重复打印保护
- 清空本机操作日志

确认框必须说明影响对象、数量和不可逆结果。

## 8.4 多工作站并发

- 当数据被其他工作站修改时，提示“数据已更新”。
- 不静默覆盖其他工作站的新版本。
- 批量入库和交仓提交前再次校验单据状态。
- 冲突时列出不可继续处理的单据，并允许返回刷新。

## 8.5 键盘和扫码枪

- 扫码输入框默认聚焦。
- 所有主要表单支持 Tab 顺序和 Enter 提交。
- 焦点环使用 2～3px 蓝色轮廓，不能被移除。
- 扫码页面的其他按钮不能无故抢走扫描输入焦点。

---

# 9. 空状态规范

不得统一使用“暂无数据”。必须区分以下原因：

| 空状态类型 | 标题示例 | 下一步 |
| --- | --- | --- |
| 从未产生数据 | 尚未录入提货单 | 录入提货单或等待客户推送 |
| 筛选无结果 | 未找到匹配结果 | 清除筛选条件 |
| 未完成配置 | 尚未选择打印机 | 前往打印机设置 |
| 无权限 | 无权查看该内容 | 联系系统管理员 |
| 服务异常 | 暂时无法获取云端数据 | 重新连接 |
| 正在同步 | 正在同步面单数据 | 显示同步进度 |
| 凭证待补 | 尚未上传完整交仓凭证 | 补充 POD 和装车照 |

每个空状态应包含：

1. 一个克制的线性图标或简单业务插画。
2. 明确标题。
3. 一句原因说明。
4. 一个主要下一步操作。

---

# 10. 加载与异常状态规范

## 10.1 加载层级

- **应用初始化：** 应用外壳保持稳定，主内容使用骨架。
- **页面查询：** 只刷新数据区域，不遮挡侧边栏和顶部栏。
- **局部查询：** 筛选按钮进入加载状态，保留旧结果直到新结果返回。
- **文件处理：** 显示阶段、数量、失败数和预计剩余工作。
- **图片加载：** 固定尺寸占位和懒加载。
- **扫码打印：** 只锁定当前任务，不显示全屏加载。

## 10.2 错误类型

- 网络不可用。
- 云端 API 不可用。
- 登录状态失效。
- 当前用户无权限。
- QZ Tray 不可用。
- 打印机不可用。
- 本地缓存不可用。
- 文件格式错误。
- 多工作站并发冲突。
- 页面组件异常。

## 10.3 错误边界

- 单个页面异常不能导致浏览器退出、返回浏览器首页或清空本机缓存。
- 显示“当前工作区遇到异常”，提供重新打开当前页面。
- 保留当前 URL，便于刷新和排查。
- 不向普通用户显示堆栈或内部密钥信息。

---

# 11. 移动端适配

## 11.1 设计断点

| 断点 | 目标设备 | 设计要求 |
| --- | --- | --- |
| 375px | 小型手机 | 单列、44px 以上触控区域 |
| 390px | 主流手机 | Stitch 主要移动画布 |
| 768px | 平板 | 两列或主从结构 |
| 1024px | 小型桌面 | 收窄侧边栏和内容间距 |
| 1440px | 标准仓库 PC | Stitch 主要桌面画布 |

## 11.2 通用规则

- 所有多列布局在 768px 以下转为单列。
- 整个页面不得产生横向滚动。
- 必须横向对比的数据表只允许表格区域内部横向滚动。
- 主要触控区域至少 44 × 44px。
- 正文至少 16px。
- 固定底部操作栏避开浏览器安全区域。
- 软键盘弹出后输入框和提交按钮保持可见。

## 11.3 表格转换

- 简单列表转为卡片。
- 提货单、账户和考勤记录使用信息分组卡片。
- 薪酬宽表保留只读摘要，完整编辑和导出转到桌面端。
- 批量选择后显示底部操作栏。

## 11.4 移动端重点能力

适合移动端：

- 我的打卡
- 入库和交仓拍照
- 凭证后补
- 提货单状态查看
- 异常申诉
- 拦截名单查看和添加
- 系统连接状态查看

不作为移动端主要能力：

- 大批 Excel/PDF 导入
- QZ 标签打印
- 复杂薪酬核算编辑
- 完整角色权限树配置

---

# 12. 无障碍与可用性

1. 正文对比度至少 4.5:1，组件边界和焦点状态至少 3:1。
2. 所有图标按钮具有明确的可访问名称。
3. 装饰性图标不重复朗读可见文字。
4. 表单字段具有真实标签、帮助文字和错误说明。
5. 状态不依赖颜色单独表达。
6. 完整支持键盘导航，Tab 顺序与视觉顺序一致。
7. Sticky 表头、底部栏和弹窗不能遮挡键盘焦点。
8. 支持浏览器缩放和系统字体放大，不禁止缩放。
9. 图片凭证提供业务含义描述，例如“入库照 1”“POD 2”。
10. 支持减少动态效果。

---

# 13. 示例数据规范

Stitch 原型使用以下具有业务意义的示例：

| 数据类型 | 示例 |
| --- | --- |
| 仓库 | JFK 测试仓 |
| 来源客户 | TYG Logistics、CM Freight |
| 标准提货单号 | 180-98109734 |
| 异常提货单号 | JFK-AIR-0829A |
| 头程单号 | HHWVP6223341301YQ |
| 末端快递单号 | LP095531854US |
| 交仓批次 | HO-1788022962 |
| 打印机 | Brother DCP-L2640DW Printer |
| 时间 | 8.29.26 18:30:26 |
| 换单进度 | 1,642 / 2,000 · 82% |
| 状态 | 已录入、已入库、已交仓、凭证待补、已拦截、已提交打印 |

不要使用无业务含义的 `Item 1`、`Order 123`、`John Doe` 或全为整百的虚构统计。

---

# 14. Stitch 原型生成顺序

为保证共享组件和视觉一致性，建议按以下顺序生成：

## 第一阶段：建立应用骨架

1. AUTH-01 登录页
2. HOME-01 工作概览
3. 全局桌面应用外壳
4. 移动端导航外壳

## 第二阶段：核心仓库业务

1. SCAN-01 扫码打单工作台
2. SCAN-02 数据导入弹窗
3. SCAN-03 拦截名单抽屉
4. AIR-01 空提管理列表
5. AIR-02 提货单录入
6. AIR-03 批量入库确认
7. AIR-04 批量交仓确认
8. AIR-05 提货单详情
9. AIR-06 交仓凭证列表

## 第三阶段：考勤薪酬

1. ATT-01 我的打卡桌面版和移动版
2. ATT-02 考勤记录
3. ATT-03 异常申诉
4. ATT-04 管理配置
5. ATT-05 薪酬核算

## 第四阶段：后台管理和设置

1. ADMIN-01 账户管理
2. ADMIN-02 角色配置
3. SET-01 打印机设置
4. SET-02 音效设置
5. SET-03 系统状态

## 第五阶段：状态补充

为以下重点页面分别生成至少一张特殊状态原型：

- 扫码打单：拦截阻断状态
- 数据导入：2,000+ 文件处理中状态
- 空提管理：筛选无结果状态
- 批量入库：实际数据有差异状态
- 批量交仓：凭证待补状态
- 我的打卡：摄像头或位置异常状态
- 薪酬核算：缺少时薪导致计算阻断状态
- 打印机设置：QZ Tray 离线状态

---

# 15. 前端验收标准

## 15.1 功能结构

- 所有屏幕均包含本文档规定的核心模块。
- 不生成未定义的营销或社交功能。
- 导航、页面标题和业务术语全局一致。
- 拦截名单位于扫码打单内部，不单独占用侧边栏导航。
- “BOL 管理”全局统一为“交仓凭证”，并归入空提管理。

## 15.2 操作效率

- 扫码打单主流程不需要鼠标。
- 批量入库和交仓主要操作不超过三步。
- 我的打卡在环境正常时十秒内可完成。
- 所有主要操作都有明确的提交中、成功和失败反馈。

## 15.3 状态完整性

- 每个数据页面至少包含正常、空、加载、错误和无权限状态。
- 状态标签同时包含文字和颜色。
- QZ 接受只显示“已提交打印”。
- 交仓凭证不足显示“凭证待补”，不能显示“凭证完整”。

## 15.4 响应式

- 375、390、768、1024、1440px 均无全页面横向滚动。
- 移动端主要触控目标不小于 44px。
- 宽表格在移动端转为卡片、摘要或局部滚动。
- 固定栏不遮挡内容和键盘焦点。

## 15.5 视觉一致性

- 所有页面使用同一颜色、字体、间距和圆角体系。
- 图标来自同一线性 SVG 家族。
- 不出现玻璃拟态、霓虹、纯黑、大面积渐变和营销型 Hero。
- 页面切换、悬停和按钮按下不会造成布局位移。

---

# 16. 原型评审检查表

交付 Stitch 原型后逐项检查：

- [ ] 桌面端应用外壳是否统一
- [ ] 当前导航和面包屑是否一致
- [ ] 扫码输入区是否是页面最明显的操作焦点
- [ ] 数据导入弹窗是否同时提供 Excel、文件夹和 ZIP 操作
- [ ] 拦截名单是否内置在扫码打单中
- [ ] 空提列表是否展示来源客户和换单进度
- [ ] 空提列表是否隐藏货物信息列
- [ ] 更新时间是否保持单行紧凑格式
- [ ] 批量入库是否展示预报与实际差异
- [ ] 批量交仓是否允许凭证待补
- [ ] 提货单详情是否展示入库照、POD 和装车照缩略图
- [ ] 我的打卡是否清晰展示摄像头、位置、网络和工作站状态
- [ ] 考勤申诉是否展示原始值和申请修正值
- [ ] 薪酬核算末列是否为“核对”
- [ ] 账户和角色页面是否体现权限差异
- [ ] QZ 离线和无打印机是否为不同状态
- [ ] 所有空状态是否包含下一步操作
- [ ] 移动端是否优先支持打卡和现场拍照
- [ ] 是否不存在无业务价值的营销元素和持续动画
