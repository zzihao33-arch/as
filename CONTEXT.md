# CM-HUB 物流对接

CM-HUB 物流对接负责接收上游客户的运单数据，并为仓库工作台和现场打印留存可追溯的业务记录。它不直接控制仓库电脑上的打印机。

## Language

**客户（Client）**：获授权通过服务端接口提交和查询自身物流数据的上游组织。
_Avoid_: 用户、账号、租户

**集成密钥（Integration API Key）**：归属于一个客户、具有明确作用域和独立生命周期的机器凭据。一个客户可以同时持有多把密钥以完成无中断轮换。
_Avoid_: 登录密码、访问令牌

**入站消息（Inbound Message）**：上游一次写入尝试的不可变接收记录，包含原始载荷、载荷哈希、幂等键和已提交响应；它与可持续更新的物流单据分离。
_Avoid_: 物流单据、Redis 缓存

**物流单据（Shipment）**：以头程单号为核心标识、可关联末端快递单号和面单文件的单个包裹记录。
_Avoid_: 订单、面单文件

**客户入站批次（Customer Inbound Batch）**：同一上游客户一次推送的完整业务批次；包含一张空运提货单预报和属于该提单的多条物流单据，客户身份由 API Key 唯一确定。
_Avoid_: 独立空提接口、仓库导入批次

**空运提货单（Air Pickup Order）**：一批头程货物从预报、入库到交仓的主单据，可关联同一客户推送的多条物流单据，并汇总展示换单处理进度。
_Avoid_: 单个包裹、BOL

**换单进度（Exchange Progress）**：空运提货单关联物流单据的处理汇总；“已换单”表示最新 QZ 结果为 `SUBMITTED`，“已拦截”表示 `BLOCKED`，均不等同于物理出纸确认。
_Avoid_: 交仓状态、物理打印成功率

**入库照片（Receipt Evidence）**：归属于一次入库批次的共享现场照片；同批次内全部空运提货单共同引用，不复制文件字节。
_Avoid_: POD、装车照

**头程单号（First-leg Tracking Number）**：上游或头程承运商用于识别物流单据的业务单号。
_Avoid_: 运单号（当上下文不明确时）

**上游订单号（Upstream Order ID）**：上游系统对订单的参考标识；它关联物流单据，但不替代用于扫码匹配的头程单号。
_Avoid_: 头程单号

**末端快递单号（Courier Tracking Number）**：末端承运商用于投递和面单匹配的快递单号。
_Avoid_: 转单号、参考单号（除非该客户模板明确如此命名）

**上游面单源（Upstream Label Source）**：上游为某一物流单据附带的兼容来源元数据；CM-HUB 云端和仓库均不抓取，不是下载合同。
_Avoid_: 仓库面单 URL、打印地址

**CM-HUB 面单资产（CM-HUB-owned Label Asset）**：由上游主动上传并经 CM-HUB 校验、私有保存，可在授权后进入仓库缓存的 PDF。
_Avoid_: 上游面单源、PDF（文件格式不是业务实体）

**仓库用户（Warehouse User）**：使用内部登录账号和密码登录 CM-HUB 工作台的自然人身份；它不等于上游客户，也不持有上游集成密钥。
_Avoid_: 客户、API 用户

**登录账号（Login Name）**：仓库用户用于身份认证的全局唯一名称；邮箱、手机号、工号和仓库代码都不是登录账号。
_Avoid_: 邮箱、工号、客户代码

**仓库成员关系（Warehouse Membership）**：把仓库用户关联到一个仓库及一个运营角色的授权事实。
_Avoid_: 浏览器权限、上游作用域

**系统管理员（System Administrator）**：可管理全部仓库用户、运营角色、权限和安全审计的全局授权；它不属于某个仓库成员关系。
_Avoid_: 仓库主管、上游客户管理员

**全局仓库可见性（Global Warehouse Visibility）**：任何有效仓库成员都可处理全部上游客户的物流单据和面单；客户归属只标识数据来源与结果回传对象，不划分仓库读取范围。
_Avoid_: 客户绑定、按仓库分租户

**工作站（Workstation）**：仓库用户登录后登记的浏览器安装实例，用于关联本机 QZ 提交审计；工作站标识不是认证凭据。
_Avoid_: 打印代理、API Key

**共享作业批次（Shared Work Batch）**：一个仓库用户导入、由所有授权工作站共同处理的 Excel 映射、面单资产和作业状态集合。
_Avoid_: 本地上传会话、上游客户、浏览器缓存

**全局拦截条目（Global Intercept Entry）**：由授权用户维护、对所有工作站立即生效的快件阻断规则。
_Avoid_: 本机拦截名单、提示信息

**上游原始载荷（Upstream Raw Payload）**：某次订单写入请求完整、未改写的 JSON 报文快照，用于对账、排错和兼容未定义字段；它不是面向现场打印的读取模型。
_Avoid_: 扩展字段、打印字段

**打印提交（Print Submission）**：现场工作站把面单交给本机打印链路的一次可审计事实；它不等于纸张已成功输出。
_Avoid_: 打印成功、已出纸

**物理打印确认（Physical Print Confirmation）**：由打印机可靠回执或人工核验支持的纸张输出事实。
_Avoid_: QZ 已接受、请求已提交

**上游结果通知（Upstream Result Notification）**：由 CM-HUB 产生并交付给上游的物流处理结果。
_Avoid_: 上游打印事件、上游状态写入

**QZ Tray 打印适配器（QZ Tray Print Adapter）**：运行在 Windows 仓库电脑的 QZ Tray 客户端，由网页通过本机 WebSocket 调用真实打印机。
_Avoid_: 云端打印服务、本地 Node 打印代理

**打卡尝试（Punch Attempt）**：仓库用户发起的一次上班或下班打卡请求；无论放行、拒绝或转为例外申请，都作为不可改写的现场事实留痕。
_Avoid_: 考勤结果、工资工时

**有效打卡（Accepted Punch）**：通过正常规则或主管批准后被采纳的上班或下班时间事实，是生成考勤日结果的输入。
_Avoid_: 打卡尝试、审批记录

**考勤凭证（Attendance Evidence）**：与打卡尝试关联的现场照片、动作检测结果、工作站和浏览器位置等辅助真实性信号；它不等同于人员身份认证。
_Avoid_: 人脸识别、登录凭据

**考勤例外申请（Attendance Exception Request）**：正常打卡条件不满足时，由员工提交并等待仓库主管处理的补充请求；在批准前不产生有效打卡。
_Avoid_: 手动打卡、已批准修正

**考勤日结果（Daily Attendance Result）**：按员工和工作日，由有效打卡、主管修正与当期考勤规则推导出的可复算结果，是薪酬计算的上游输入。
_Avoid_: 原始打卡、工资单

**薪酬计算结果（Payroll Calculation Result）**：按薪酬周期和规则版本，从考勤日结果、时薪、奖金与油补等输入推导出的工资计算快照。
_Avoid_: 打卡记录、工资发放

**薪酬档案身份快照（Payroll Identity Snapshot）**：员工登录身份被删除后，与法定工时和薪酬档案共同保留的最小姓名、工号及仓库归属快照；它不是可恢复的账号或完整员工档案。
_Avoid_: 离职账号、账户归档、员工通讯录
